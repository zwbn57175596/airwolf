spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:zwbn57175596/spring-study-config-repo.git
          search-paths: src/main/resources
logging:
  file: ./config-server.log
  level: info
server:
  port: 8901
management:
  security:
    enabled: false

