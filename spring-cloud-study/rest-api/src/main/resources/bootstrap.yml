spring:
  cloud:
    config:
      label: master
      uri: http://localhost:8901/
      profile: dev
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: springconfig
    password: springconfig

endpoints:
  bus:
    enabled: true
    sensitive: false
